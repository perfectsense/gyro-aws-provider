aws::webhook webhook
    build-type: "BUILD"
    rotate-secret: true

    project
        name: "project-example-name"
    end

    filter-groups
        pattern: "PUSH"
        type: "EVENT"
    end
end
